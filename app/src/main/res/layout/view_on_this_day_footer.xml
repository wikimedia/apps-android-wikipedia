<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/linear_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/paper_color"
    android:orientation="vertical">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/end_of_scroll_message"
        style="@style/FlatButton.Small.Transparent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:clickable="false"
        android:text="@string/end_of_scroll_message"
        android:textAllCaps="false"
        android:textColor="?attr/primary_text_color"
        android:textSize="20sp"
        android:textStyle="bold"
        app:iconGravity="textEnd"
        app:iconSize="20dp"
        app:iconTint="?attr/primary_text_color"
        app:layout_constraintStart_toEndOf="@id/addButton"
        app:layout_constraintTop_toTopOf="@id/addButton" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/back_to_future_text_view"
        style="@style/TransparentButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:text="@string/back_to_future"
        android:textAllCaps="false"
        android:textColor="?attr/colorAccent"
        android:textSize="20sp"
        android:textStyle="bold"
        app:icon="@drawable/ic_baseline_arrow_up_alt_24"
        app:iconGravity="end"
        app:iconSize="20dp"
        app:iconTint="?attr/colorAccent"
        app:layout_constraintStart_toEndOf="@id/addButton"
        app:layout_constraintTop_toTopOf="@id/addButton" />
</LinearLayout>
