<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <com.skydoves.balloon.vectortext.VectorTextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="24dp"
        android:fontFamily="sans-serif-medium"
        android:text="@string/suggested_edits_image_recommendations_task_title"
        android:textAllCaps="true"
        android:textSize="14sp"
        android:textColor="@android:color/white"
        android:letterSpacing="0.04"
        android:layout_marginBottom="4dp"
        android:gravity="center_vertical"
        android:drawablePadding="8dp"
        app:drawableStart="@drawable/ic_article_images"
        app:drawableTint="@android:color/white"/>

    <org.wikipedia.views.AppTextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="@android:color/white"
        android:lineSpacingExtra="7sp"
        app:html="@string/suggested_edits_image_recommendations_onboarding_tooltip"/>

</LinearLayout>
