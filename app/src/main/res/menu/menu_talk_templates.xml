<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_new_message"
        android:title="@string/talk_templates_new_message_title"
        android:icon="@drawable/ic_add_gray_white_24dp"
        app:iconTint="?attr/progressive_color"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />
    <item android:id="@+id/menu_remove_messages"
        android:title="@string/talk_templates_menu_remove_message"
        android:icon="@drawable/ic_delete_white_24dp"
        android:visible="false"
        app:iconTint="?attr/primary_color"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />
    <item
        android:id="@+id/menu_overflow"
        android:icon="@drawable/ic_more_vert_white_24dp"
        android:title="@string/nav_item_more"
        app:iconTint="?attr/primary_color"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/menu_enter_remove_message_mode"
                android:title="@string/talk_templates_menu_remove_message"/>
        </menu>
    </item>
</menu>